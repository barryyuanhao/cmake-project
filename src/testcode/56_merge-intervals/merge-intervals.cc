#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<vector<int>> merge(vector<vector<int>>& intervals);
};

vector<vector<int>> merge(vector<vector<int>>& intervals) {

}

int main(int argc, char* argv[]){
    
    return 0;
}